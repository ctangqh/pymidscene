# WinAppDriver 地址（默认本地，无需修改）
WINAPPDRIVER_HOST=127.0.0.1
WINAPPDRIVER_PORT=4723

# MCP 服务器监听地址（SSE 模式）
MCP_HOST=0.0.0.0
MCP_PORT=55001

# WinAppDriver HTTP 请求超时时间（秒）
WINAPPDRIVER_HTTP_TIMEOUT=120

# 日志级别
LOG_LEVEL=INFO

# 是否自动启动 WinAppDriver
WINAPPDRIVER_AUTO_START=true
